node_modules/
build/
.svelte-kit/
dist/
src-tauri/target/
src-tauri/gen/
.DS_Store
*.log

docs/

/distnet/

# Updater signing keys
*.key
izumi_updater.key
.env.local

# Native libmpv binaries staged into src-tauri/ at build time (bundled by CI, not committed)
src-tauri/*.dll
src-tauri/*.dylib

# Local Flatpak bundle artifact (built + deployed, not committed)
izumi.flatpak
