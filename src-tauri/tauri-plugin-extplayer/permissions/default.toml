"$schema" = "schemas/schema.json"

[default]
description = "Allow Android playback, self-update, and power/network checks for responsible seeding."
permissions = ["allow-play-external", "allow-install-apk", "allow-device-status"]
