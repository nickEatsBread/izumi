"$schema" = "schemas/schema.json"

[default]
description = "Allow launching an external Android video player and the package installer for self-update."
permissions = ["allow-play-external", "allow-install-apk"]
